spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://localhost:5433/${PG_DATABASE:jira}-test
    username: ${PG_USERNAME:jira}
    password: ${PG_PASSWORD:JiraRush}